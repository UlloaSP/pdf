# Desbloquear PDF

Desbloquea usando una contraseña válida. No descifra por fuerza bruta. También copia documentos sin cifrado. La salida contiene el documento sin protección.

Pruebas: `python -m unittest discover -s engine/tests -p test_unlock.py -v`.
